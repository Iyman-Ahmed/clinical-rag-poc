# Pinned for reproducibility (clone-and-run on a clean machine).

# --- core (required) ---
chromadb==1.5.9
sentence-transformers==5.6.0
openai==2.44.0
fastapi==0.115.6
uvicorn==0.34.0

# --- optional: legacy Streamlit UI (app.py). Not needed for the FastAPI app. ---
streamlit==1.58.0
